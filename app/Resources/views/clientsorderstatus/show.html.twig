{% extends 'base.html.twig' %}

{% block body %}
    <h1>ClientsOrderStatus</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clientsOrderStatus.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ clientsOrderStatus.title }}</td>
            </tr>
            <tr>
                <th>Desctiption</th>
                <td>{{ clientsOrderStatus.desctiption }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('clientsorderstatus_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('clientsorderstatus_edit', { 'id': clientsOrderStatus.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
