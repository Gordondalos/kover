{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProductPropertiesValue</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productPropertiesValue.id }}</td>
            </tr>
            <tr>
                <th>Propertiesvalue</th>
                <td>{{ productPropertiesValue.propertiesValue }}</td>
            </tr>
            <tr>
                <th>Properties</th>
                <td>{{ productPropertiesValue.properties }}</td>
            </tr>
            <tr>
                <th>Product</th>
                <td>{{ productPropertiesValue.product }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('productpropertiesvalue_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('productpropertiesvalue_edit', { 'id': productPropertiesValue.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
